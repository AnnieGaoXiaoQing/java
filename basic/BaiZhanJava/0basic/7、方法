1、方法重载（overload）：
    方法名称相同，参数不同，可以进行区分
    方法名相同，参数个数不同，构成重载
    方法名相同，参数类型不同，构成重载
    方法名相同，参数顺序不同，构成重载
    以下情况，不构成重载
        只有返回值不同
        只有参数名称不同
2、递归：自己调自己
    两部分：
        递归头：什么时候不调用自己
        递归体：什么时候调用自身方法
    递归：效率低
3、构造方法（construct）
    通过new创建
    构造器虽然有返回值，但不能定义返回值类型，不能在构造器中使用return返回某个值
    若自己定义构造器，系统则不会生成无参构造器
    构造器方法名必须与类名一致
4、方法重写（override）：子类重写父类
    目标：子类的行为替换父类的行为
5、Object类：所有类的根基类
   所有的java对象都拥有Object类的属性和方法
   toString():
6、 == 和 equals
   ==：基本类型则值相等，引用类型则表示地址相等
   equals：对象内容相等
7、super是直接父类引用
   可通过super来访问父类中被子类覆盖的方法或属性
   所有构造方法：第一句都是调用super(),调用父类的构造方法
   流程：先向上追溯到Object，然后再依次向下执行类的初始化和构造方法，直到当前子类为止
8、



