1、方法重载（overload）：
    方法名称相同，参数不同，可以进行区分
    方法名相同，参数个数不同，构成重载
    方法名相同，参数类型不同，构成重载
    方法名相同，参数顺序不同，构成重载
    以下情况，不构成重载
        只有返回值不同
        只有参数名称不同
2、递归：自己调自己
    两部分：
        递归头：什么时候不调用自己
        递归体：什么时候调用自身方法
    递归：效率低
3、构造方法（construct）
    通过new创建
    构造器虽然有返回值，但不能定义返回值类型，不能在构造器中使用return返回某个值
    若自己定义构造器，系统则不会生成无参构造器
    构造器方法名必须与类名一致
4、

